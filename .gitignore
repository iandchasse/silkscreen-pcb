EPD_Hub_ESP32-backups
minRead_pcb-backups
silkscreen_pcb-backups
/ordering script

# LTspice raw simulation output (can be huge)
*.raw

# Per-user project state and regenerated caches (KiCad recommends not committing these)
*.kicad_prl
fp-info-cache

# KiCad autosave, lock and backup files
_autosave-*
~*.lck
\#auto_saved_files\#
*.bak
*-bak

# Generated fabrication output — regenerate with kicad-cli (see fabrication/README.md).
# BOM, assembly CSVs and the how-to ARE tracked; the gerber/drill artifacts are not.
fabrication/gerbers/
fabrication/*.zip

# Output/cache folders created by other fab-house plugins
nextpcb/
database/

# Fabrication Toolkit per-run copies and local order records
production/backups/
production/*.xls
production/*.xlsx
production/bom-JLCPCB
production/bom_JLC_upload-JLCPCB
